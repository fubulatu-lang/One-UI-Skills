---
description: "Full One UI conformance audit across all nine areas"
argument-hint: "<path or areas>"
---

Use the `one-ui-audit` skill.

Target: $ARGUMENTS

If no target is given, work on the current repository root.

Before starting, read `reference/PLATFORMS.md` to detect the platform and
`reference/TOKENS.md` for the token values. Do not guess the platform — ask if
the markers are ambiguous.
